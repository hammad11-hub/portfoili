# Workspace Instructions

This workspace contains a starter MERN portfolio project.

- `backend/` includes Express server and MongoDB model
- `frontend/` includes React portfolio UI
- Use `npm install` and `npm start` in each folder to run the app
